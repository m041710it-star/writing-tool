# CHANGELOG

このファイルには、本プロジェクトの変更内容をバージョンごとに記録します。
（日付が新しいものを上に追記していきます）

## v6 (2026-09-04)

概要: サイドバー表示の調整（見出し削除、モデル説明文の分離、案内文言の修正、メニューの余白）

詳細:
- サイドバー上部の「⚙️ 設定」という見出し文字を削除
- モデル選択プルダウンの選択肢からカッコ書きの説明文（軽量・低コストなど）を外し、プルダウンのすぐ下にキャプションとして表示するように変更。選択を切り替えると説明文も連動して切り替わる
- 「有料枠: 未設定」の案内リンクの文言を「切り替えはこちら（⚙️ 設定ページへ）」から「設定はこちら」に修正（リンク先は変更なし）
- サイドバーのページメニューに、トップページ/ブログ記事作成の間、タイトル生成/翻訳の間、翻訳/設定の間の3箇所に1行分の余白を追加（`st.navigation` のグループ分けを利用し、見出しテキストは非表示のまま余白のみ挿入）

## v5 (2026-09-04)

概要: 無料枠の利用状況表示を追加し、429エラー時の案内をモデル別・段階的な内容に拡充

詳細:
- サイドバー・「📊 API利用状況」ページに「無料枠」「有料枠」の状態を常に併記して表示するように変更（`render_usage_status()`）。無料枠はGemini APIに残量取得の仕組みがないため、直近のAPI呼び出しで429が発生したかどうかをもとにした簡易表示（✅利用可能 / ⚠️利用制限に達した可能性）にとどめる
- 有料枠はテキスト形式の進捗バー（`[■■■□□□□] 42%`）と残高目安（円）の表示に変更。「有料利用に切り替えました」トグルは無料枠の状態に関わらずいつでもON/OFFできる位置づけを維持
- 429エラー発生時、選択中モデルの相対的な負荷傾向（軽量・標準・高負荷）に応じて案内を出し分けるように変更。高負荷・標準モデルでは軽量モデルへワンクリックで切り替えられるボタンを、最軽量モデルでは無料枠のリセット時間の目安を表示し、いずれも有料化の案内（支出上限の設定推奨・公式リンク・課金額の反映手順）を添える
- モデルの相対的な軽量度判定・案内文の出し分けロジックを追加した `utils/models.py` を新設（`utils/common.py`・`utils/gemini_client.py`共通で利用）。特定モデルが無料枠対象外だと断定はせず、あくまで目安である旨をコード・案内文の両方でヘッジ
- 無料枠に関する表示・案内文すべてに「現時点（2026年9月時点）での簡易的な目安であり、最新の条件はGoogle公式サイトで確認してほしい」旨と[Gemini API 料金ページ](https://ai.google.dev/gemini-api/docs/pricing)へのリンクを追加

## v4 (2026-09-03)

概要: トップページの無料枠前提の注記を削除、API利用状況表示を無料枠/有料の2状態にシンプル化、仕様書ページの追加、トップページのデザイン改善

詳細:
- トップページから「本アプリはGemini APIの無料枠での利用を前提としています」という注記を削除
- API利用状況の表示を「無料枠を利用中（デフォルト）」「有料利用中（『⚙️ 設定』ページのトグルをONにした場合のみ）」の2状態にシンプル化。無料枠利用中はサイドバーに簡潔な案内のみを表示し、進捗バーは非表示に。有料利用中に切り替えた場合のみ、課金履歴の入力欄・進捗バー・残高目安が有効になる
- 429エラー（クォータ超過）時のメッセージに、無料枠利用中のみ「支出上限（予算アラート）」の事前設定を推奨する案内と公式ドキュメントへのリンク、課金した場合の反映手順を追加
- 現時点の全機能を利用者向けにまとめた仕様書（`SPEC.md`）を追加し、「📘 仕様書」ページから閲覧できるように（「📜 変更履歴」と同様の位置づけでメニューに追加）
- トップページに猫のワンポイントSVGイラストと絵文字を添え、親しみやすい雰囲気に変更（機能・リンクの動作に影響なし）

## v3 (2026-09-03)

概要: メニュー構成の刷新、設定ページの認証追加、各ページの入力項目改善、API利用状況の可視化機能を追加

詳細:
- トップページを正方形カードから簡潔な箇条書き形式に変更
- サイドバーメニューを「トップページ」が最上位になるよう再構成し、APIキー入力欄を専用の「設定」ページに移動
- 「設定」ページに簡易パスワード認証を追加（他ページは引き続き認証なし）
- デフォルトの使用モデルをGemini Flash-Liteに変更
- 変更履歴をメニューから直接閲覧できるページを追加
- 各ページの入力欄・出力欄にトークン数を表示する機能を追加
- 「創造性（temperature）」の表現を「創造性（表現の幅）」に変更し、説明文を追加
- ブログ記事作成: 記事タイプをプルダウン化、トーンにカスタム入力を追加、テーマとタイトル案の入力欄を分離
- メール返信作成: トーンにカスタム入力を追加、長さの表現を具体化、相手との関係性に応じた文体生成を追加
- トーン変換ページを「文体変換」に改称し、分かりにくかった項目名を修正
- タイトル生成ページの表記を統一
- 校正・リライトページに、任意のチェック項目を入力してその観点でチェックできる機能を追加
- 生成のたびにトークン数・概算コストを記録する利用履歴と、課金履歴・単価設定・残高目安を管理する「API利用状況」ページを追加
- サイドバーに、課金残高に対する使用割合を示す進捗バー（80%で警告色、100%超で赤）を追加

## v2 (2026-08-30)

概要: ブログ記事作成のSEOモード追加と、無料枠を意識したAPI設計・UI微調整

詳細:
- ブログ記事作成に「note記事」「Webブログ記事（SEO重視）」の2モードを追加し、SEO向けの入力項目（メインキーワード・関連キーワード・ペルソナ・見出し数・文字数目安）と出力（タイトル案・メタディスクリプション・FAQ）を追加
- Webブログ記事は本文・タイトル案・メタディスクリプション・FAQなどを1回のAPIリクエストにまとめて生成するように変更（無料枠のリクエスト回数を抑制）
- 429エラー（クォータ超過）時に、より軽量なモデルへの切り替えを案内するエラーメッセージを追加（全ページ共通）
- SEO記事生成のシステムプロンプトに、キーワードスタッフィング対策の指示を明記
- トップページに無料枠利用前提の注記を追加し、サイドバーのモデル選択肢を無料枠で動かしやすい軽量モデル優先の構成に変更

## v1 (初期リリース)

概要: 7つのライティング機能を備えた初期バージョンをリリース

詳細:
- ブログ記事作成・メール返信作成・文章要約・校正リライト・トーン変換・タイトル生成・翻訳の7機能を実装
- Gemini APIキーの設定方法として、サイドバー入力・secrets.toml・.env の3通りに対応
