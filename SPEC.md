# 仕様書（最新版）

このアプリが現時点で持つ全機能を、利用者向けにまとめたものです。
機能追加・変更を行うたびに、このファイルも更新していきます（変更の経緯は「📜 変更履歴」ページを参照してください）。

最終更新: 2026-09-04（サイドバー表示調整）

## このアプリについて

Gemini APIを使った、個人利用向けの日本語ライティング支援ツールです。データベースは使わず、すべてブラウザのセッション内で完結します。生成した文章は各ページのダウンロードボタンから保存してください。

「⚙️ 設定」ページのみパスワードで保護されています。それ以外のページに認証はありません。

## メニュー構成（サイドバー上から順に）

- 🏠 トップページ — この画面。使える機能の一覧を紹介します。
- 📝 ブログ記事作成 / ✉️ メール返信作成 / 📄 文章要約 / ✏️ 校正・リライト / 🎨 文体変換 / 💡 タイトル生成 / 🌐 翻訳 — 7つのライティング機能。
- ⚙️ 設定 — Gemini APIキーの入力、課金モードの切り替え（パスワード保護）。
- 📊 API利用状況 — トークン数・概算コストの履歴と、課金履歴・残高目安の管理。
- 📘 仕様書 — このページ。
- 📜 変更履歴 — バージョンごとの変更内容。

## 各機能ページの詳細

### 📝 ブログ記事作成

記事タイプを2つから選べます。

- **note記事（エッセイ・体験談風）**: トーン（丁寧・解説調／カジュアル・親しみやすい／専門的・硬め／ユーモアを交えて／カスタム自由入力）、想定読者、含めたいキーワード、文章量の目安（短め・標準・長め）、含めたい構成要素（導入・見出し・具体例・まとめ・CTA・FAQなど複数選択）、その他の指示を入力して生成します。
- **Webブログ記事（SEO重視）**: メインキーワード、関連キーワード、想定読者（ペルソナ）、見出し（H2）の数、文字数の目安、FAQの有無、その他の指示を入力すると、タイトル案・メタディスクリプション・本文・FAQをまとめて1回で生成します。

どちらのモードでも、記事のテーマ・タイトル案（任意）を入力できます。

### ✉️ メール返信作成

受信したメール本文と、返信で伝えたい要点を入力すると返信文を作成します。トーン（ビジネス・フォーマル／丁寧だが柔らかい／カジュアル／謝罪・お詫び／お礼／カスタム自由入力）、長さ、相手との関係性（上司・同僚・部下・取引先・友人）、署名（任意）を指定できます。

### 📄 文章要約

要約したい文章を貼り付けるか、テキストファイル（txt/md）をアップロードします。要約の長さ（一言で／短め／標準／詳しめ）、出力形式（文章／箇条書き）、特に重視したい観点（任意）を指定できます。

### ✏️ 校正・リライト

モード（誤字脱字チェックのみ／分かりやすく言い換え／簡潔にする／丁寧・フォーマルにする／説得力を強める）を選んで実行します。修正点の説明を出力するかどうかを選べるほか、独自のチェック項目（1行1項目、任意）を入力すると、その観点ごとに指摘の有無をチェックします。

### 🎨 文体変換

文章の口調（ビジネス敬語／カジュアル・フランク／フレンドリー／SNS向け／丁寧語／学術的）を変換します。元の文章の長さをできるだけ変えずに変換するオプションがあります。

### 💡 タイトル生成

記事の内容やキーワードから、タイトル案を生成します。スタイル（SEO重視／クリックしたくなる／シンプルで誠実／SNS向け）、生成する案の数（3〜15個）を指定できるほか、記事内で使えるh2見出し案を一緒に生成することもできます。

### 🌐 翻訳

文章を指定した言語（英語・日本語・中国語（簡体字／繁体字）・韓国語・フランス語・ドイツ語・スペイン語・ポルトガル語・その他自由入力）に、トーン（自然な標準表現／ビジネス・フォーマル／カジュアル）を保ったまま翻訳します。

## 共通設定（サイドバー）

すべてのページ共通で、サイドバーから以下を設定できます。

- **使用するモデル**: Gemini Flash-Lite（デフォルト）／Flash／Proから選択。選んだモデルの説明（軽量・低コスト、高速・バランス型、高精度・じっくり、など）はプルダウンのすぐ下に表示され、選択を切り替えると連動して変わります。
- **創造性（表現の幅）**: 0〜2のスライダー。値が高いほど自由で多様な文章に、低いほど堅実で一貫した文章になります（校正・リライトなど一部の機能では、内部的に固定値を使う場合があります）。
- **APIキー設定状況**: キーが設定されているかどうかの表示。
- **API利用状況（無料枠・有料枠）**: 「📊 API利用状況（※簡易的な目安です）」という見出しのもと、無料枠・有料枠それぞれの状態を常に並べて表示します（詳しくは下記「📊 API利用状況ページ」を参照）。

## ⚙️ 設定ページ

- パスワード認証あり。`SETTINGS_PASSWORD` が未設定の場合、このページ自体が利用できません。
- **Gemini APIキー**の入力欄（このセッション中のみ保持、`.env`や`secrets.toml`で設定済みの場合は上書きに使えます）。
- **課金モードの切り替え**: 「有料利用に切り替えました」トグル。デフォルトはOFF（無料枠利用中）。実際にGoogle Cloud側で有料アカウントへ切り替えた後にONにすると、「📊 API利用状況」ページの課金履歴入力欄・残高目安の表示が有効になります。

## 📊 API利用状況ページ

- **無料枠・有料枠の状況**: サイドバーと同じ内容をページ本文でも確認できます。
  - **無料枠**: Gemini APIには「残量」を取得する仕組みがないため、正確な残量ではなく「直近のAPI呼び出しでエラーが発生していないか」を目安として表示します。
    - 直近が成功していれば「無料枠: ✅ 利用可能（直近でエラーなし）」
    - 直近で429（クォータ超過）が発生していれば「無料枠: ⚠️ 利用制限に達した可能性があります（目安）」
  - **有料枠**: 「⚙️ 設定」ページで「有料利用に切り替えました」をONにしていない間は「有料枠: 未設定」と表示し、切り替え用のリンクを示します。ONにして課金履歴を入力すると、`[■■■□□□□]` 形式のテキスト進捗バーと使用率（%）、残高目安（円）を表示します（80%で注意、100%超で警告のキャプションを追加表示）。
  - いずれも「これは現時点（2026年9月時点）での簡易的な目安であり、最新の条件はGoogle公式サイトで確認してほしい」旨の注記を添えます。
- **課金履歴**: 有料モードのときのみ、日付と金額を記録・一覧表示できます。
- **単価設定**: 入力・出力それぞれのトークン単価（100万トークンあたりの円）を設定できます（デフォルトはGemini Flash-Liteの目安単価）。
- **利用履歴**: これまでの生成ごとのトークン数・概算コストの記録、日ごとの小計、機能（ページ）ごとの小計を一覧表示します。

このページで扱う数値はすべてアプリ内で計算した目安であり、実際の請求額・無料枠の条件はGoogle Cloud／Google AI Studio／[Gemini API 料金ページ](https://ai.google.dev/gemini-api/docs/pricing)の公式情報でご確認ください。

## 429エラー（クォータ超過）時の案内

Gemini APIのレート制限・クォータの上限に達すると、エラーメッセージが表示されます。あわせて、直近の呼び出し状態が「無料枠: ⚠️」に切り替わります。

無料枠利用中は、「※これは簡易的な目安です」と断ったうえで、**そのとき選択していたモデルの相対的な負荷傾向**に応じて案内を出し分けます（特定モデルが無料枠の対象外だと断定するものではなく、あくまで目安です）。

- **比較的高性能・高負荷なモデル（例: Pro）の場合**: 「無料枠の対象外、または制限が厳しい可能性がある」旨を案内し、より軽量なモデルへ切り替えるボタンと、有料化の案内（下記）を表示します。
- **標準的なモデル（例: Flash）の場合**: 「アクセス集中または無料枠の利用制限に達した可能性がある（目安）」旨を案内し、より軽量なモデルへ切り替えるボタン、しばらく待ってからの再試行の推奨、有料化の案内（下記）を表示します。
- **すでに最も軽量なモデル（例: Flash-Lite）の場合**: 「最も軽量なモデルでも制限に達しているため、本日の無料枠の利用上限に達した可能性が高い（目安）」旨を案内し、無料枠がリセットされる時間の目安（日本時間で夕方頃、太平洋時間の深夜0時。季節等により変動）と、有料化の案内（下記）を表示します（これ以上の軽量モデルへの切り替え提案はしません）。

いずれの場合も、有料化の案内には次を含めます。

- 想定外の高額請求を防ぐため、先にGoogle Cloudで「支出上限（予算アラート）」を設定してから有料利用に切り替えることを推奨する旨と、[設定方法（Google Cloud公式）](https://cloud.google.com/billing/docs/how-to/budgets)へのリンク
- 実際に課金した場合は、「⚙️ 設定」ページのトグルをONにした上で「📊 API利用状況」ページの課金履歴に金額を入力すると、サイドバーに残高目安が表示されるようになる旨
- 「⚙️ 設定」ページへのショートカットリンク

有料利用中は、この無料枠向けの案内は表示せず、モデル切り替えなど簡潔な案内のみを表示します。

## 📜 変更履歴ページ

`CHANGELOG.md` の内容をそのまま表示します。バージョンごとの変更内容を確認できます。

## 📘 仕様書ページ（このページ）

この `SPEC.md` の内容をそのまま表示します。機能追加・変更を行うたびに更新する運用です。

## データの扱いについて

- データベースは使用せず、生成結果や入力内容はブラウザのセッション内でのみ保持されます（再読み込みすると消えます）。
- 利用履歴・課金履歴・単価設定・課金モードは、プロジェクト直下の `usage_data.json` にローカル保存されます（外部送信なし、Gitの管理対象外）。
- 生成結果は各ページのダウンロードボタンからテキストファイルとして保存できます。
