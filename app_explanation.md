# 📝 このコード、何をしているの？ `app.py` を読み解くブログ

Streamlitで作られたAIライティングツールの「ホーム画面」にあたるファイルです。実際に画面を作っている処理を上から順に見ていきましょう。

---

## 1. まずは道具を持ってくる（インポート）

```python
import streamlit as st
from utils.common import render_sidebar
```

- `streamlit`（`st`）は、Pythonだけでブラウザ上のWebアプリを作れるライブラリです。HTMLやJavaScriptを書かなくても、`st.title(...)` のような関数を呼ぶだけで画面部品が並びます。
- `render_sidebar` は、このプロジェクトの `utils/common.py` に自分たちで定義した関数です。「サイドバーの設定UI（APIキー入力・モデル選択など）」をひとまとめにして、どのページからも使い回せるようにしています。

## 2. ページの基本設定

```python
st.set_page_config(
    page_title="AI ライティングツール",
    page_icon="🖋️",
    layout="wide",
)
```

これはブラウザのタブに表示されるタイトルやアイコン（🖋️）、画面レイアウトを決める設定です。`layout="wide"` にすることで、画面いっぱいを使う横広レイアウトになります。

> ⚠️ Streamlitのルールとして、`set_page_config()` はそのページで一番最初に呼ぶ必要があります。

## 3. サイドバーを表示する

```python
render_sidebar()
```

たった1行ですが、中身は `utils/common.py` にあります。ここでAPIキーの入力欄・使うGeminiモデルの選択・temperature（創造性）のスライダーがサイドバーに描画されます。すべてのページで同じサイドバーを使い回すために、共通関数として切り出しているわけです。

## 4. タイトルと説明文

```python
st.title("🖋️ AI ライティングツール")
st.caption("Gemini APIを使った個人用の文章作成アシスタント集")
```

`st.title` は大きな見出し、`st.caption` はその下にグレーの小さい説明文を出します。

## 5. 使い方の案内（Markdown）

```python
st.markdown(
    """
左のサイドバーからページを選んで使いたい機能を開いてください。
はじめに **サイドバーでGemini APIキー** を設定する必要があります
...
"""
)
```

`st.markdown` はMarkdown記法（`**太字**` など）がそのまま反映されるテキスト表示です。ユーザーに「まずAPIキーを設定してね」と案内しています。

```python
st.divider()
```

これは単なる区切り線（`---`のようなもの）です。見た目を整理するために使われています。

## 6. ツール一覧をデータとして持つ

```python
tools = [
    ("📝", "ブログ記事作成", "テーマとキーワードから..."),
    ("✉️", "メール返信作成", "..."),
    ...
]
```

ここがちょっとしたポイントです。「アイコン・名前・説明文」の3つを1セットにした**タプル**を、リストにまとめています。

- if文やst.writeを7回ベタ書きするのではなく、データと表示処理を分離している
- 新しいツールを追加したくなったら、このリストに1行足すだけでいい

という、地味だけど読みやすく・拡張しやすい書き方になっています。

## 7. カード形式で一覧表示（ループ処理）

```python
cols = st.columns(2)
for i, (icon, name, desc) in enumerate(tools):
    with cols[i % 2]:
        with st.container(border=True):
            st.subheader(f"{icon} {name}")
            st.write(desc)
```

一番ロジックらしいロジックがここです。分解すると：

1. `st.columns(2)` → 画面を**2列**に分割する
2. `enumerate(tools)` → リストの中身を「番号（i）」と「中身（icon, name, desc）」に分けて1個ずつ取り出す
3. `i % 2` → `i`を2で割った余り。つまり `0, 1, 0, 1, 0, 1, 0` と交互になる → **偶数番目は左列、奇数番目は右列**に配置される、というジグザグ配置のテクニックです
4. `st.container(border=True)` → 枠線付きの箱を作る（＝「カードUI」に見せている）
5. その箱の中に `st.subheader`（見出し）と `st.write`（説明文）を入れる

つまり「7個のツールを、2列のカードとして自動的に並べる」処理を、たった5行で実現しています。

## 8. 最後の注意書き

```python
st.divider()
st.caption(
    "本ツールは個人利用向けのため、ユーザー認証やデータベースへの保存は行っていません。"
    "生成結果は各ページでダウンロードして保存してください。"
)
```

区切り線のあと、「認証やDB保存はない」という注意点をキャプションで表示して締めています。

---

## まとめ

| やっていること | 使っているStreamlit機能 |
|---|---|
| 画面の基本設定 | `set_page_config` |
| サイドバー共通部品の呼び出し | 自作の `render_sidebar()` |
| 見出し・説明文 | `title` / `caption` / `markdown` |
| ツール一覧をデータ化 | 普通のPythonのリスト・タプル |
| 2列カード表示 | `columns` + `container(border=True)` + ループ |

このファイル自体は**表示専門**で、実際にAIへ文章生成を依頼する処理（Gemini APIを呼ぶ部分）は行っていません。それは `pages/` フォルダの各ツールファイル（`utils/gemini_client.py` の `generate_stream()` を呼ぶ側）が担当しています。`app.py` はあくまで「玄関＝メニュー画面」の役割だと考えると全体像がつかみやすいと思います。
